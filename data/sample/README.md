# Sample data

The repository does not ship copied live NHTSA complaint records. Use `recallzero demo` for a generated synthetic smoke test, or `recallzero fetch` to retrieve public NHTSA data into the local cache.
